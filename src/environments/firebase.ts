export const firebase = {
    // Paste your config object from the firebase console here
    // Project Overview -> click "Add app" and select WEB
    // select a name and copy the firebaseConfig object here
    // (just the text inside the {...})
};
